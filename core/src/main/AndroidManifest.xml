<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <service
            android:name="dashingineering.jetour.tboxcore.service.TboxHostService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="dashingineering.jetour.tboxcore.HOST_SERVICE" />
            </intent-filter>
        </service>
    </application>
</manifest>